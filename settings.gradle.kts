pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "zekes-world"

include(":core")
include(":desktop")
include(":android")
